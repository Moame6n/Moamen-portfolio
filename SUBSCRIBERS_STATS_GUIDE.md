# 📊 دليل إحصائيات المشتركين

## نظرة عامة
تمت إضافة ميزة جديدة شاملة لتتبع وإدارة إحصائيات المشتركين في الموقع بشكل آمن وسهل.

## المكونات المضافة

### 1. **API Endpoints** (`/api`)

#### `subscription-log.js` - تسجيل أحداث الاشتراك
**الدور:** تسجيل كل حدث اشتراك أو إلغاء اشتراك  
**الطريقة:** POST  
**المعاملات:**
```javascript
{
  action: 'subscribe' | 'unsubscribe',
  subscription: {
    endpoint: 'string',
    keys: { p256dh: 'string', auth: 'string' }
  },
  userName: 'string (اختياري)',
  userEmail: 'string (اختياري)'
}
```

**الاستجابة:**
```javascript
{ success: true, message: 'تم الاشتراك بنجاح' }
```

#### `subscription-stats.js` - جلب الإحصائيات
**الدور:** إرجاع إحصائيات شاملة عن المشتركين  
**الطريقة:** GET  
**الاستجابة:**
```javascript
{
  total: 152,              // المشتركون النشطون الحاليون
  today: 5,               // من اشترك اليوم
  last7days: 28,          // آخر 7 أيام
  unsubscribed: 12,       // ألغوا الاشتراك
  recent: [               // آخر 20 مشترك
    {
      id: 'uuid',
      user_name: 'محمد',
      user_email: 'ahmed@example.com',
      created_at: '2026-09-05T...',
      status: 'active'
    }
  ],
  dailyStats: [           // آخر 14 يوم
    { date: '2026-09-05', count: 3 },
    { date: '2026-09-04', count: 7 }
  ]
}
```

### 2. **جدول Supabase** (`push_subscriptions`)
**الحقول:**
- `id` - معرّف فريد (UUID)
- `endpoint` - رابط الاشتراك (مفتاح فريد)
- `p256dh` - مفتاح التشفير
- `auth` - مفتاح المصادقة
- `user_name` - اسم المستخدم
- `user_email` - البريد الإلكتروني
- `status` - حالة الاشتراك (active/unsubscribed)
- `created_at` - تاريخ الاشتراك
- `updated_at` - آخر تحديث

### 3. **واجهة المستخدم في لوحة التحكم**

تم إضافة تبويب جديد: **📬 إحصائيات المشتركين**

#### المكونات:
1. **بطاقات الإحصائيات** (4 بطاقات):
   - المشتركون النشطون
   - اشتركوا اليوم
   - آخر 7 أيام
   - ألغوا الاشتراك

2. **رسم بياني النمو اليومي**:
   - يعرض البيانات لآخر 14 يوم
   - بار لكل يوم بارتفاع يمثل عدد المشتركين الجدد
   - يظهر عند التمرير فوقه تاريخ والعدد الدقيق

3. **جدول آخر المشتركين الجدد**:
   - الاسم (أو "مستخدم مجهول" إذا لم يتم توفيره)
   - البريد الإلكتروني (إن وجد)
   - تاريخ الاشتراك
   - حالة الاشتراك (✓ نشط أو ✕ ملغي)

## الأمان 🔒

### 1. **حماية البيانات الحساسة**
- لا يتم عرض `endpoint`, `p256dh`, أو `auth` في لوحة التحكم
- فقط المعلومات العامة (الاسم، البريد، التاريخ) تُعرض

### 2. **حماية الوصول**
- جميع العمليات في لوحة التحكم محمية بكلمة المرور نفسها
- لا يمكن الوصول إلى الإحصائيات بدون تسجيل الدخول

### 3. **معايير قاعدة البيانات**
- Row Level Security (RLS) مفعّل على جدول `push_subscriptions`
- الموقع العام يستطيع INSERT فقط، لا SELECT

## الاستخدام

### من الموقع العام (تسجيل الاشتراك):

```javascript
// عند الاشتراك
await fetch('/api/subscription-log', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    action: 'subscribe',
    subscription: pushSubscription, // من Service Worker
    userName: userFullName,
    userEmail: userEmail
  })
});

// عند إلغاء الاشتراك
await fetch('/api/subscription-log', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    action: 'unsubscribe',
    subscription: pushSubscription
  })
});
```

### من لوحة التحكم:
1. ادخل لوحة التحكم بكلمة المرور
2. انقر على تبويب **📬 إحصائيات المشتركين**
3. شاهد الإحصائيات الحية والرسوم البيانية وآخر المشتركين الجدد

## الإحصائيات المتاحة

| الإحصائية | الوصف | الاستخدام |
|---------|-------|---------|
| **المشتركون النشطون** | عدد الأشخاص الذين لم يلغوا اشتراكهم | تتبع الوصول الفعلي |
| **اشتركوا اليوم** | عدد المشتركين الجدد في آخر 24 ساعة | قياس النشاط اليومي |
| **آخر 7 أيام** | عدد الاشتراكات الجديدة في الأسبوع الماضي | تتبع الاتجاهات الأسبوعية |
| **ألغوا الاشتراك** | عدد من ألغوا اشتراكهم | قياس معدل الخسارة |
| **الرسم البياني** | توزيع الاشتراكات اليومية (14 يوم) | تصور النمو والأنماط |

## ملاحظات مهمة

### للمطورين:
- الـ API endpoints تُستخدم في `/api/` (Vercel Functions)
- البيانات تُخزّن في Supabase مع تشفير كامل
- لا توجد بيانات حساسة في الاستجابات العامة

### للإدارة:
- تحديث تلقائي للإحصائيات عند الضغط على التبويب
- البيانات تُحسب من الحالة الفعلية في قاعدة البيانات
- لا توجد بيانات مخزنة مسبقًا (computed on-demand)

### للمستخدمين:
- اشتراك/إلغاء آمن تمامًا وآني
- البيانات الشخصية لا تُعرّض بدون الحاجة
- يمكنك إلغاء الاشتراك في أي وقت

## خطوات التطبيق على Vercel

1. **تأكد من متغيرات البيئة**:
   ```
   SUPABASE_URL=your_url
   SUPABASE_SERVICE_ROLE_KEY=your_key
   ```

2. **اختبر الـ APIs**:
   ```bash
   curl https://your-domain.vercel.app/api/subscription-stats
   ```

3. **اختبر من الموقع**:
   - فعّل الإشعارات من الموقع
   - شاهل الإحصائيات في لوحة التحكم

## استكشاف الأخطاء

### المشكلة: لا تظهر الإحصائيات
**الحل**: 
- تأكد من تسجيل الدخول لوحة التحكم
- تحقق من console للأخطاء
- تأكد من أن متغيرات البيئة صحيحة

### المشكلة: الأرقام لا تتطابق
**الحل**:
- قد تكون هناك تأخيرات في المزامنة
- احذف الكاش (Ctrl+Shift+R)
- تحقق من جدول `push_subscriptions` مباشرة

### المشكلة: API يرجع خطأ
**الحل**:
- تحقق من logs في Vercel
- تأكد من صلاحيات Supabase
- تحقق من أن جدول `push_subscriptions` موجود

---

**آخر تحديث**: 5 سبتمبر 2026  
**الإصدار**: 1.0  
**الحالة**: ✅ منتج ومجهز للاستخدام
